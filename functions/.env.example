# Local Functions emulator only — use .secret.local (not committed) for defineSecret overrides.
# Production uses Google Cloud Secret Manager via:
#   firebase functions:secrets:set GEMINI_API_KEY
#
# Create functions/.secret.local with:
#   GEMINI_API_KEY=your-gemini-api-key
